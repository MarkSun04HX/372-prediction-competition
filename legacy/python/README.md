# Legacy Python pipeline

Course work now uses **R** (`scripts/*.R`, `R/`). These files are kept for reference only:

- `process_meps_for_modeling.py`, `pool_meps_parquets.py`, `run_linear_baselines.py`, `expand_competition_exclusions.py`
- `src/` — exclusion helpers

See the root **README.md** for the R workflow.
